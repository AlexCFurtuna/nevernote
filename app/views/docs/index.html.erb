<div class="container">
  <div class="section-one">
    <div class="meetings" id="meetings">
      <h1>Meetings</h1>
      <div class="">
        <% @meetings.each do |meeting| %>
        <a href="<%= (url_for [meeting]) %>">
        <ul>
          <li id="m-name"><%= truncate(meeting.name, length:30) %></li>
          <li id="m-time"><%= meeting.starts_at.strftime("%l:%M %p, %b %e, %Y") %></li>
        </ul>
        <% end %>
        <button id="new"><%= link_to 'New Meeting', new_meeting_path %></button>

      </div>
      <!--<table>
        <thead>
          <tr>
            <th>Name</th>
            <th>Starts at</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @meetings.each do |meeting| %>
            <tr>
              <td><%= meeting.name %></td>
              <td><%= meeting.starts_at %></td>
              <td><%= link_to 'Show', meeting %></td>
              <td><%= link_to 'Edit', edit_meeting_path(meeting) %></td>
              <td><%= link_to 'Destroy', meeting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>-->
    </div>
    <div class="wrapper_with_padding" id="wrapper_w_padding">
      <div id="sticky">
        <h1>Ola! Here are your notes!</h1>
      </div>
      <div class="clearfix" id="docs">
        <% unless @docs.blank? %>
          <% @docs.each do |doc| %>
            <a href="<%= (url_for [doc]) %>">
              <ul class="doc">
                <li class="title"><%= truncate(doc.title, length:30) %></li>
                <li class="data float-right"><%= doc.created_at.strftime("%a, %b %e, %Y") %></li>
                <!--<li class="data float-right">Last Updated: <%= doc.updated_at.strftime("%a, %b %e, %Y") %> ago</li>-->
                <li class="content">
                <!--  <span>  <%= truncate(doc.content, length:50) %></span>-->
                </li>
              </ul>
            </a>
          <% end %>
        <% else %>
          <h2>Create Doc!</h2>
          <p>Start Creating Documents and Organizing your Cabinet!</p>
          <button>
            <%= link_to "Create Doc", new_doc_path %>
          </button>
        <% end %>
      </div>
    </div>
  </div>

  <div class="two">
    <div id="toHide" class="section-two two">
      <h1>Did you forget something hombre?</h1>
      <h2>Well put your sombreros on because from now on with NeverNote you shall not forget anything again</h2>
      <p> <button id="trigger" type="button" name="button"> <a>Click here</a> </button> and discover our new NeverNote Calendar nEvent </p>
    </div>

    <!--<button type="button" name="button"> <a href="">Hold you sombreros because this ride is going to be bumpy!</a> </button>-->

    <div class="section-two" id="section-two">
      <%= render 'calendar' %>
    </div>
  </div>
<script type='text/javascript'>

</script>
<style media="screen">

</style>
